{
  "name": "my-nodejs-app",
  "version": "1.0.0",
  "description": "A sample Node.js application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "mkdir -p dist && cp -r archetypes/* content/* layouts/* themes/* static/* dist/"
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
